import {createTheme} from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "dark",
    background: {default: "#2c2c2c"},
    primary: {
      main: "#ffffff",
    },
    secondary: {
      main: "#454545",
    },
  },
});
